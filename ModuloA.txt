Modulo A - Conhecimento Js
Aula: Website precisa:

- CCS: Disegn da pagina, parte visual e artistica, formatos e cores.
- HTML: Texto e imagens, tamanho de titulos e fontes, paragrafos e linhas.
- JS: Stack, tras funcionalidade e movimento ao site. Nomear e definir objetos.

Comando: Ctrl + L = limpar console.
Span: Caracter que pode ser adicionado no codigo.

EcmaScript é o Js padronizado.

Para desenvolver um Software é preciso:

- Navegador
- Visual Studio Code

 Node Js é uma stack usada no back-end

 Aula 05

 - window.alert() é usado para inserir um pop up na tela escrito algo com um botão para selecionar
 - window.confirm() é usado para inserir um pop up na tela com duas opções para selecionar
 - window. prompt() é usado para inserir um pop up com um box para escrever algo

 >> COMENTARIOS JS]

 // unica linha

 /* quando deseja comentar 
    mais de uma linha */

 >> VARIAVEL 

  SIMBOLOS EM JS

  = receber 
  null nenhum
  var variavel 
  " " delimitar string
  ' ' delimitar string
  ` ` delimitar string (interpolar)

  >> IDENTIFICADORES 

  -letra, $ ou _

FORMATO DE STRINGS

'Estou aprendendo s'  // não faz interpolação 
'Estou aprendendo' + s  // usa concatenação
`Estou aprendendo ${s}` // usa template string

s.length   // quantos caracteres a string tem
s.toUppperCase()  //muda tudo para letra mauscula
s.toLowerCase()  //tudo para 'minuscula'
E da para fazer muitos outros

n1.toFixed(2).replace('.',',') // usado para alterar casas decimais e trocar ponto por virgula.
n1.toFixed(4) // usado para alteração as casas decimais